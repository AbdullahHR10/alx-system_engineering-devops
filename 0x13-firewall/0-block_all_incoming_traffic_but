# Update package list
sudo apt-get update

# Install ufw
sudo apt install -y ufw

# Allow ssh
sudo ufw allow ssh

# Allow Tcp ports
sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

# Enable firewall
sudo ufw enable
